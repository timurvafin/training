{
  "name": "training-tracker-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "HTML-трекер тренировок (Apps Script + Google Sheets) — тесты чистой логики",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=23.6"
  }
}
