# Скопируй в .env и впиши свои значения (.env в .gitignore, в публичный репо не попадает).
# ID Google-таблицы, в которую трекер пишет факт тренировок (из URL таблицы).
SPREADSHEET_ID=your-google-sheets-id-here
